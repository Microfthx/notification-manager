# 更新日志

## v0.2.0 - 2026-09-18

### NapCat QQ 管理

- 首页增加 NapCat QQ 状态卡，显示账号、版本、最近在线时间和连续检测失败次数。
- 每 30 秒检查 OneBot `online` 与 `good` 状态，连续 3 次失败后确认离线。
- 离线告警通过已启用的非 NapCat 通道发送，相同接收端去重，同类告警 6 小时内最多发送一次。
- QQ 离线时支持调用 NapCat WebUI 重启内部工作进程，并在管理页面使用手机 QQ 扫码恢复登录。
- NapCat 继续由宿主机 `tmux:napcat` 会话托管；WebUI Token 通过只读挂载读取，不暴露给前端。

### 结构化配置

- 查询任务与推送通道改为两个独立的单项编辑区，避免同时展开全部配置。
- 下拉选项显示名称、类型及启用状态，并默认选择第一个已启用的任务或通道。
- 新增项目后自动切换到新项，删除后自动选择相邻项。
- 任务的目标推送通道改为从已启用通道中勾选；通道禁用、改名或删除时同步清理任务引用。

## v0.1.0 - 2026-08-02

### 管理与运行

- 前端默认端口调整为 `10010`，后端 API 使用 `4000`。
- 增加机器人创建、配置编辑、启动、停止、重启和日志查看入口。
- 增加“自动拉起”滑动开关，配置持久化到机器人目录；后端启动后自动恢复已勾选机器人，并每 15 秒检查一次运行状态。
- 修复机器人 stdout/stderr 共用日志流时的 `write after end` 崩溃，避免后端被 Docker 意外重新拉起。
- 前后端容器使用 `restart: unless-stopped`，随 Docker 开机启动。

### 微博会话

- 增加服务端 Chromium 持久登录档案，默认每 4 小时检查登录态并同步 Cookie。
- 增加微博扫码登录、扫码状态轮询、过期重试和取消操作。
- 登录成功后自动更新所有微博机器人配置，核心登录凭据变化时重启运行中的机器人。
- 忽略 `_T_WM`、XSRF 等短期 Cookie 的轮换，避免无意义的周期性机器人重启。
- Cookie 配置采用原子写入，文件权限为 `600`；浏览器档案权限为 `700`。

### 配置与兼容

- 机器人通过 `AIO_CONFIG_PATH` 读取各自的 `aio-config.yml`，不再误用 AIO 项目根配置。
- YAML 输出统一 Cookie 和时间字段的双引号格式。
- 修复微博任务时间值被 YAML 解析为数字后产生的类型比较错误。
- 后端使用 host 网络访问本机 NapCat，解决 `localhost:3000` 连接失败。

### 日志与界面

- 日志按最新到最旧显示，同时保留 Python traceback 内部顺序。
- 前端、后端和机器人统一使用 `Asia/Shanghai` 时区。
- 增加中英文界面、机器人配置表单、微博会话状态卡和响应式布局。

### 安全

- Git 忽略 `.env`、机器人 Cookie、运行日志、浏览器登录档案、构建产物和运行时机器人实例。
- API 和日志不会返回 Cookie 明文，账号标识仅显示末四位。
